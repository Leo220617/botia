services:
  - type: worker
    name: educonnect-bot
    env: python
    plan: free
    buildCommand: |
      echo "🧹 Limpiando entorno viejo..."
      rm -rf .venv
      pip install --upgrade pip
      pip uninstall -y python-telegram-bot || true
      pip install --no-cache-dir python-telegram-bot==20.8 requests==2.32.3
    startCommand: "python bot_educonnect.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
