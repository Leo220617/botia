services:
  - type: worker
    name: educonnect-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot_educonnect.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9   # 👈 MUY IMPORTANTE — versión estable compatible
