services:
  - type: worker
    name: educonnect-bot
    env: python
    plan: free
    buildCommand: |
      echo "🧹 Eliminando entornos anteriores..."
      rm -rf venv_bot
      python3 -m venv venv_bot
      venv_bot/bin/pip install --upgrade pip
      venv_bot/bin/pip install --no-cache-dir python-telegram-bot==20.8 requests==2.32.3
    startCommand: "venv_bot/bin/python bot_educonnect.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
